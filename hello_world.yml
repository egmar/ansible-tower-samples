- name: describe-hosts
  hosts: all
  tasks:
   - name: describe hosts
     debug: msg="Computer {{ ansible_hostname }} with bios version {{ ansible_bios_version }} is running {{ ansible_os_family }} or equivalent"
